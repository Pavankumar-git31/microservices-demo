apiVersion: apps/v1
kind: Deployment
metadata:
  name: shippingservice-deployment
  namespace: boutique
  labels:
    app: shippingservice   
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shippingservice   
  template:
    metadata:
      labels:
        app: shippingservice 
  spec:
    containers:
    - name: shippingservicecontainer
      image: pavankumarkothala31/shippingservice:v1
      ports:
        - containerPort: 50051
      resources:
        requests:
          cpu: "100m"
          memory: "64Mi"
        limits:
          cpu: "200m"
          memory: "128Mi"

      readinessProbe:
        initialDelaySeconds: 10
        periodSeconds: 10
        grpc:
          port: 50051
      livenessProbe:
        initialDelaySeconds: 10
        periodSeconds: 10
        grpc:
          port: 50051
      env:
      - name: PORT
        value: "50051"
      - name: DISABLE_PROFILER
        value: "1"
       

---
apiVersion: v1
kind: Service
metadata:
  name: shippingservice   
  namespace: boutique
spec:
  type: ClusterIP
  selector:
    app: shippingservice 
  ports:
  - name: grpc
    port: 50051
    targetPort: 50051
